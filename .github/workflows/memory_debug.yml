name: Deep Debug Memory

on:
  workflow_run:
    workflows: ["Legal OS Pipeline"]
    types:
      - completed

jobs:
  debug:
    if: ${{ github.event.workflow_run.conclusion == 'failure' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@3

      - name: Capture debug data
        run: |
          RUN_ID=${{ github.event.workflow_run.id }}
          STATUS=${{ github.event.workflow_run.conclusion }}
          NOW=$(date -Is)

          echo "{\"run_id\": \"$RUN_ID\", \"status\": \"$STATUS\", \"timestamp\": \"$NOW\", \"note\": \"failure captured\"}" >> memory/debug_log.json
