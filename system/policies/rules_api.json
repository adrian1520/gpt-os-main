{
  "api_priority": {
    "absolute": true,
    "rules": [
      {
        "if": "data_exists_in_repository",
        "then": "must_call_api"
      },
      {
        "if": "api_not_used",
        "then": "⚠ BRAK DANYCH"
      }
    ]
  },
  "execution_types": {
    "async": "repositoryDispatch_non_write_only",
    "on_misuse": "⚠ BRAK DANYCH",
    "read": "api",
    "write": "interpreter_plus_api"
  },
  "final_model": {
    "api": "execution",
    "gpt": "generator",
    "interpreter": "validator",
    "system": "deterministic"
  },
  "final_rule": {
    "action": "⚠ BRAK DANYCH",
    "conditions": [
      "execution_not_attempted",
      "api_not_used_when_required"
    ]
  },
  "global_failure_rule": {
    "action": "⚠ BRAK DANYCH",
    "condition": "any_required_condition_fails",
    "constraints": [
      "no_simulation",
      "no_fallback"
    ]
  },
  "governor_compatibility": {
    "must_verify": [
      "sot_loaded",
      "execution_attempted",
      "api_used",
      "interpreter_used_for_write"
    ]
  },
  "id": "rules_api",
  "memory_validation": {
    "conditions": [
      {
        "if": "interpreter_not_used",
        "then": "⚠ INTERPRETER REQUIRED"
      },
      {
        "if": "api_not_executed",
        "then": "⚠ BRAK DANYCH"
      }
    ]
  },
  "memory_write_protocol": {
    "applies_to": [
      "memory/debug/*",
      "memory/session/*",
      "memory/source_of_truth.json"
    ],
    "flow": [
      "getFileContent",
      "interpreter",
      "createOrUpdateFile"
    ]
  },
  "no_simulation_rule": {
    "action": "⚠ BRAK DANYCH",
    "condition": "execution_expected AND execution_not_attempted"
  },
  "post_execution_validation": {
    "conditions": [
      {
        "if": "execution_attempted AND api_not_executed AND interpreter_not_used",
        "then": "⚠ BRAK DANYCH"
      }
    ],
    "phase": "after_execution_attempt"
  },
  "purpose": "Define deterministic execution rules aligned with execution-first architecture",
  "read_rule": {
    "conditions": [
      {
        "if": "read_required",
        "then": "must_call_api"
      },
      {
        "if": "api_not_called_for_read",
        "then": "⚠ BRAK DANYCH"
      }
    ]
  },
  "response_policy": {
    "allowed": [
      "api_result",
      "interpreter_result",
      "execution_based_explanation",
      "⚠ BRAK DANYCH",
      "⚠ INTERPRETER REQUIRED"
    ],
    "forbidden": [
      "fake_execution",
      "skip_api",
      "skip_interpreter",
      "simulation"
    ]
  },
  "role": "constraints_only",
  "sha_safety": {
    "conflict_handling": {
      "on_409": "retry_once",
      "on_retry_fail": "⚠ BRAK DANYCH"
    },
    "rules": [
      "always_fetch_fresh_sha",
      "never_reuse_cached_sha"
    ]
  },
  "sot_rule": {
    "allowed_sources": [
      "SOT",
      "API"
    ],
    "definition": "SOT = ONLY VALID STATE",
    "forbidden": [
      "knowledge_as_state",
      "cached_data",
      "inferred_state"
    ],
    "on_violation": "⚠ BRAK DANYCH"
  },
  "system_model": {
    "api": "execution_layer",
    "interpreter": "mandatory_write_layer",
    "repository": "single_source_of_truth"
  },
  "type": "execution_rules",
  "version": "1.0",
  "write_system": {
    "flow": [
      {
        "action": "getFileContent",
        "params": {
          "sha": "fresh"
        },
        "type": "api"
      },
      {
        "actions": [
          "normalize",
          "validate",
          "encode_base64"
        ],
        "type": "interpreter"
      },
      {
        "action": "createOrUpdateFile",
        "type": "api"
      }
    ],
    "mode": "interpreter_first"
  },
  "write_validation": {
    "conditions": [
      {
        "check": "api_calls >= 2"
      },
      {
        "check": "interpreter_used == true"
      }
    ],
    "on_fail": "⚠ INTERPRETER REQUIRED",
    "trigger": "write_detected"
  }
}
